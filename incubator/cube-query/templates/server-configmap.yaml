{{- with .Values.server.extraConfig.usersConf }}

apiVersion: v1
kind: ConfigMap
metadata:
  name: users-conf
  labels:
{{ include "cube-query.labels" . | indent 4 }}
data:
  users.cfg: |
    {{- toYaml . | nindent 4 }}

{{- end }}
